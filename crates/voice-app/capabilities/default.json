{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Windows may listen to the app's events, query their own visibility and drag themselves by their data-tauri-drag-region chrome. The app's own commands need no grant (no app manifest is declared); autostart is driven from Rust.",
  "windows": ["main", "onboarding", "overlay"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging"
  ]
}
