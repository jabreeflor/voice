# build.sh copies the bundled app here for install.sh / e2e-smoke.sh
Voice.app/

# Whisper models — downloaded on first run, too big for git
models/

# macOS
.DS_Store

# Rust build output, Tauri's generated schemas, agent worktrees
target/
crates/voice-app/gen/
.claude/worktrees/

# whisper.cpp built from source by CI (Linux/Windows jobs)
whisper-cpp-src/
whisper-cpp-bin/
